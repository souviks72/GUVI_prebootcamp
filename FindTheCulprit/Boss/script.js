// You cant change the value of the msg
var message;
if (message === null || message === 2 || message === undefined )
{
    message = "welcome boss";
}
else
{
    message = "Go away";
}
  console.log(message);